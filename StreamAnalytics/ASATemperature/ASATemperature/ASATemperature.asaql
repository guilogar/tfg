SELECT
  sensorId,
  AVG(temperature) AS temperature,
  AVG(humidity) AS humidity,
  COUNT(*) AS count
INTO
  [CosmosDB]
FROM
  [IoTHub]
GROUP BY
  sensorId, TumblingWindow(minute, 2)
HAVING
  AVG(temperature) > 20
